<h1>Hangman</h1>
<p>Try to guess the secret word by typing single letters!</p>

<%= form_with do |form| %>
    <%= form.text_field :letter %>
    <%= form.submit "Submit" %>

<% end %>

<%= @secret_word %><br>
<%= @board.join %>